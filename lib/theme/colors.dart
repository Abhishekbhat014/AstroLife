import 'package:flutter/material.dart';

class AppColors {
  // A calm, sophisticated Teal theme
  static const Color primary = Color(0xFF00796B);
  static const Color accent = Color(0xFFFFC107); // A vibrant amber
  static const Color background = Color(0xFFE0F2F1); // A very light teal
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF263238); // Blue-gray dark text
  static const Color textSecondary = Color(0xFF607D8B); // Blue-gray medium text
}
